# Security Notes

CipherBox does not yet expose a complete authenticated encryption container. The current module contains only password-derived key material helpers. Do not use those primitives as a file encryption format.
